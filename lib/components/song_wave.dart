import 'package:flutter/material.dart';

class WaveSong extends StatelessWidget {
  const WaveSong({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.waves);
  }
}